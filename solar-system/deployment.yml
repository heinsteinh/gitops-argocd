apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: solar-system
  name: solar-system
spec:
  replicas: 3
  selector:
    matchLabels:
      app: solar-system
  strategy: {}
  template:
    metadata:
      labels:
        app: solar-system
    spec:
      containers:
      - image: fkheinstein/solar-system:v1-armd64
        name: solar-system
        imagePullPolicy: Always
        ports: 
        - containerPort: 80
